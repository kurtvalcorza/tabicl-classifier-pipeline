# TabICLv2 Classifier — DIMER Pipeline

DIMER pipeline for fine-tuning TabICLv2 on tabular classification datasets.

This umbrella repository contains the pipeline documentation plus synchronized copies of the deployable validator and fine-tuner sources. The deployable repositories are:

- `tabicl-classifier-dataset-validator`
- `tabicl-classifier-finetuner`
